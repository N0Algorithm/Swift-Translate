import React from 'react';

const HeroSection = () => {
  return (
    <section className="hero-section animate-fade-in" style={{ animationDelay: '0.1s' }}>
      <h1 className="hero-title font-display-hero">Translate without limits.</h1>
      <p className="hero-subtitle font-body-lg">
        Professional-grade translation powered by advanced AI. Clean, fast, and secure.
      </p>
    </section>
  );
};

export default HeroSection;
